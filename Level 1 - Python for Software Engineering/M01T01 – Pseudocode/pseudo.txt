Q1
create an empty list named positive numbers to store the positive numbers
ask user to enter a positive number
while input does not equal 0
      if number is greater than 0
        add number to positive numbers
        ask user to enter a positive number
      else if number is less than 0
        ask user to enter a positive number
output the largest number in positive numbers
print largest number

Q2  
ask user for their name
store input in first_name
print "Hello, World" + first_name

Q3
get integer list
set integer total to 0
set counter to 1 
for each number in integer list
  add number in the integer list to integer total
  add 1 to counter
calculate the average by dividing the integer total by counter 
print integer average

Q4
get groceries list
create an empty list named groceries left
for each product in grocery list
  if product not bought
    add product to groceries left
  else if product bought
    continue
sort groceries left aphabetically
print groceries left

Q5
get x 
get y
variable s equals add x and y together
variable a equals s divided by 2
variable p equals x times y 
print s
print a
print p
